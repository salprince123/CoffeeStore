<UserControl x:Class="CoffeeStore.View.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CoffeeStore.View"
             mc:Ignorable="d"
             d:DesignWidth="800"
             Loaded="UserControl_Loaded" Height="40">
    <Grid Background="#6B6158">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Name="Title" Text="{Binding Path=Title}" />
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button BorderThickness="0" Height="Auto" Background="#6B6158" Name="CloseButton" Click="CloseButton_Click">
                <materialDesign:PackIcon Kind="Close" Height="33" Width="33" />
            </Button>
        </StackPanel>
    </Grid>

</UserControl>
